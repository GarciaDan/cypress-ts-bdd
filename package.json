{
  "name": "cypress-ts-bdd",
  "version": "1.0.0",
  "description": "Simple example of a test project using Cypress with Typescript and BDD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Garcia",
  "license": "MIT",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.1.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^12.3.0",
    "typescript": "^4.9.4"
  },
    "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/**/*.ts",
      "cypress/e2e/*.ts"
    ]
  }
}
